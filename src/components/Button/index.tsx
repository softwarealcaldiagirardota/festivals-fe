import { Button } from "@mui/material";

const ButtonMaterial = () => {
  return (
    <Button
      variant="contained"
      color="primary"
      sx={{ color: "#fff", border: "20px" }}
    >
      Contained
    </Button>
  );
};

export default ButtonMaterial;
