import styled from "@emotion/styled";

const StyledHeader = styled("div")(() => ({
  margin: "90px 0 0 0",
  width: "100%",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
}));

export { StyledHeader };
